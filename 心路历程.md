# 视频转 MP3 智能分类工具 · 开发心路历程

## 缘起：一次简单的需求

起初只是想要一个能将视频批量转换为 MP3 的小工具，顺便按照文件名中的“-”自动创建多级目录，比如 `中文歌-陈奕迅-屋顶.mp4` 变成 `中文歌/陈奕迅/屋顶.mp3`。

第一个版本很快完成：一个 Tkinter 图形界面，列出视频文件，支持全选/取消，点击“转换全部”调用 ffmpeg，默认输出到本地“歌曲”文件夹。

## 第一版痛点：手动移动太麻烦

用了一段时间，发现每次都要手动将生成的 MP3 文件从“歌曲”文件夹拖到目标音乐库，很烦。于是新增了“移动歌曲”功能，允许用户选择一个目标目录，程序会把“歌曲”文件夹内的所有 mp3（保留目录结构）移动过去。

但用户反馈：既然目标目录已经固定，为什么不能直接从源头输出到那里，省去移动步骤？

## 第二版：直接输出到目标目录

增加了 `config.json` 的 `default_target_dir` 配置项。如果用户填写了目标目录，转换时跳过本地“歌曲”文件夹，直接输出到目标目录的对应子文件夹下（如 `E:\音乐\中文歌\陈奕迅\屋顶.mp3`）。未填写时则沿用旧逻辑。

同时，用户希望“歌曲”文件夹在移动完成后能被自动清理，避免残留空文件夹。于是新增 `delete_song_folder_after_move` 配置，移动后自动删除空的“歌曲”文件夹。

## 第三版：保护配置，避免误操作

早期配置是简单的 `config.json`，但若代码更新导致配置格式变动，旧配置会被覆盖。为了安全，实现了备份机制：每次重建 `config.json` 前，若存在旧文件，则备份为 `backup_config.json`（冲突则先删除旧备份再复制）。这样用户永远不会丢失手写的配置。

## 第四版：适应不同用户习惯

部分用户不希望“-”被解析成目录分级，他们需要文件名原样输出（例如 `周杰伦-七里香.mp4` 直接变成 `七里香.mp3`）。于是增加了一个新配置 `enable_prefix_classify`，并在界面上增加“使用“-”作为目录分级”的复选框（默认勾选）。关闭后，所有“-”当作普通字符，文件输出到根目录。

同时，为了照顾完整分类需求的用户，保留了`path_mappings`（歌手快捷映射）功能，允许用户定义短名称对应深层路径，例如 `“jay” → “流行/周杰伦/国语”`，在文件名只包含两个“-”分段时自动补全。

## 第五版：界面微调和响应式

按钮文字从啰嗦的“一键智能转换全部视频”简化为“转换全部视频”；窗口默认全屏最大化；所有提示窗口居中显示，信息精简；状态栏实时显示进度。

## 最终的关键设计原则

1. **保持简单**：默认行为只需一键操作，高级选项通过配置界面调节。
2. **不丢数据**：每次配置变更前自动备份。
3. **灵活分级**：既可自动创建多级目录，也可关闭分级输出为平面文件。
4. **路径可定制**：支持用户自定义目标根目录和歌手映射，满足复杂的音乐库组织需求。

## 当前功能一览

- 批量转换视频文件（mp4/avi/mkv 等）为 MP3
- 开启分级：文件名中的“-”自动创建多级目录（可依据歌手映射补全）
- 关闭分级：所有 MP3 输出到同一文件夹
- 自动删除原视频（可选）
- 支持目标目录直接输出（跳过本地临时文件夹）
- 移动已有歌曲到目标目录，并自动清理本地空文件夹
- 配置自动备份（backup_config.json）
- 图形界面全屏、中文支持、实时状态提示

---

## 展望

未来可能会增加：
- 拖拽支持
- 批量设置元数据（ID3 标签）
- 预览转换后的目录结构

但核心已经足够稳定，能满足日常视频转音乐的需求。这个项目，从一个简单的脚本变成了一个考虑周全、可配置化的工具，见证了用户反馈驱动的小步迭代如何带来质的飞跃。
